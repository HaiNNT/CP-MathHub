@using CP_MathHub.Models.Discussion
@model List<CategoryPreviewViewModel>
@{

    foreach (CategoryPreviewViewModel category in Model)
    //for (int i = 0; i < Model.Count; i++ )
    {
        @Html.Partial("Partials/_CategoryPreviewPartialView", category);
    }
}
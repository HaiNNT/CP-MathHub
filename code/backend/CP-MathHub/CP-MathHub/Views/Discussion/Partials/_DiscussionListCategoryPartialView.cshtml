@using CP_MathHub.Models.Discussion
@model ICollection<DiscussionTagPreviewViewModel>
@{

    for (int i = 0; i < Model.Count; i++)
    {
        @Html.Partial("Partials/_DiscussionCategoryPreviewPartialView", Model.ElementAt(i));
    }
}
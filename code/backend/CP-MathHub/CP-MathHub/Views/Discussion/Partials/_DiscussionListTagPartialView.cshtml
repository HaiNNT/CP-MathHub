@using CP_MathHub.Models.Discussion
@model ICollection<DiscussionCategoryViewModel>
@{

    //foreach (DiscussionCategoryViewModel tag in Model)
    for (int i = 0; i < Model.Count; i++ )
    {
        @Html.Partial("Partials/_DiscussionTagPreviewPartialView", Model.ElementAt(i));
    }
}
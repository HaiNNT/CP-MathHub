@using CP_MathHub.Models.Question
@model ICollection<QuestionPreviewViewModel>
<h2>Number: @Model.Count</h2>
@{

    foreach (QuestionPreviewViewModel question in Model)
    {
        @Html.Partial("Partials/_QuestionPreviewPartialView", question);
    }
}

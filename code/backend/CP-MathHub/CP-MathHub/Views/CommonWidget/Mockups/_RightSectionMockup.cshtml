<div class="mh-banner">
    <a href="#">
        <img src="~/Content/img/CTV.jpg">
    </a>
</div>
@if (ViewBag.System != null)
{
    <div class="mh-btn">
        <a href="@Url.Action("Create", ViewBag.System)" class="btn btn-success">@(ViewBag.System == "Question" ? "Đặt câu hỏi" : @ViewBag.System == "Blog" ? "Tạo bài viết" : "Tạo thảo luận")</a>
        @*@if (@ViewBag.System == "Discussion")
        {
            @if (@ViewBag.MhPage == "discussion-home")
            {
                <a href="@Url.Action("Create", ViewBag.System, new { })" class="btn btn-succes">Tạo Thảo Luận</a>
            }
            else
            {
                <a href="@Url.Action("Create", ViewBag.System, new { })" class="btn btn-succes">Tạo Thảo Luận</a>
            }
        }*@
    </div>
}
else
{
    <div class="mh-btn">
        <a href="@Url.Action("Create", ViewData["System"].ToString(), new { @tagId = (ViewData["tagId"] != null? ViewData["tagId"] : "0")})" class="btn btn-success">@(ViewBag.System == "Question" ? "Đặt câu hỏi" : @ViewBag.System == "Blog" ? "Tạo bài viết" : "Tạo thảo luận")</a>
    </div>
}

